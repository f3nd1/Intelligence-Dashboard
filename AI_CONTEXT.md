# AI Context - UCC Intelligence Platform v1.9.5

## Current source of truth

Use the `src` files and Server Scripts in this package. The compiled `custom-html-block` and `dist` files are generated by `tools/build_custom_block.py`.

## Confirmed UCC mappings

- Staff Goal analytics: `Goal`
- Training needs: `Training Needs Analysis`
- Communication material approval: `Material Vetting Form`
- Provider evaluation: `Provider Rating`, with `Supplier Rating` retained only as an approved fallback in Criteria 3 and 6

Do not reintroduce obsolete Criterion 2 source aliases. Use only the confirmed mappings listed above.

## Visual catalogue contract

The approved counts are 84, 99, 90, 84, 94, 96 and 80 for Criteria 1 to 7 respectively. The Explorer and section hover menus must use approved visual definitions, not every chart node present in legacy HTML.
